# Dependencies
node_modules/

# Secrets — never commit
.env
.env.*
!.env.example

# Playwright
/test-results/
/playwright-report/
/blob-report/
/playwright/.cache/
allure-results/
allure-report/

# TypeScript
*.tsbuildinfo

# Terraform
infra/**/.terraform/
infra/**/*.tfstate
infra/**/*.tfstate.*
infra/**/crash.log
infra/**/*.tfplan
# .terraform.lock.hcl is deliberately NOT ignored — like pnpm-lock.yaml, it pins exact
# provider versions and should be committed for reproducible plans/applies.

# OS / editor
.DS_Store
Thumbs.db
.vscode/*
!.vscode/extensions.json
.idea/

# Logs
*.log
npm-debug.log*
pnpm-debug.log*

# Personal step-by-step run manual — not shared, local reference only
E2E_manual.md

# Personal Claude Code project instructions — not shared, local reference only
CLAUDE.md
